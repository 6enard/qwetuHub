import React from 'react';
import { SprayCan as SprayCanIcon } from 'lucide-react';

const SprayCan = ({size = 24}: {size?: number}) => {
  return <SprayCanIcon size={size} />;
};

export default SprayCan;